# @uynis/cli

Uynis command-line interface for workspace, repository, token, admin, and automation operations.

Local usage in monorepo:

```bash
pnpm cli --help
```

Standalone usage:

```bash
node bin/uynis.mjs --help
```

---
Written by Krishnam Murarka (km@edilec.com)

